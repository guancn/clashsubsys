# 环境变量配置示例文件
# 复制此文件为 .env 并根据需要修改

# 端口配置 - 用户可自定义
BACKEND_PORT=8000
FRONTEND_PORT=3000
NGINX_PORT=80
NGINX_HTTPS_PORT=443

# 后端配置
API_BASE_URL=http://localhost:8000

# 数据库配置（如果需要）
# DATABASE_URL=sqlite:///./clash_converter.db

# 日志级别
LOG_LEVEL=INFO

# CORS 配置
CORS_ORIGINS=*

# SSL 配置（如果使用 HTTPS）
SSL_CERT_PATH=/etc/nginx/ssl/cert.pem
SSL_KEY_PATH=/etc/nginx/ssl/key.pem